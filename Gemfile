source "https://rubygems.org"

# Lock to GitHub Pages environment
gem "github-pages", group: :jekyll_plugins

# Windows compat shims
platforms :mingw, :x64_mingw, :mswin, :jruby do
  gem "tzinfo", ">= 1", "< 3"
  gem "tzinfo-data"
end
